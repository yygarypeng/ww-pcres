# Tuned k-fold config: trial 5 of the constrained_v2 sweep (sweep/outputs_v2).
# Fold-1 confirmation score 0.0444 vs 0.1431 untuned, feasible on every seed.
# "untuned:" marks values that differ from untuned_kfold_config.yaml.
# The sweep scored 60-epoch checkpoints; re-check the bias after full training.
parameters:
  seed: 2330
  folds: 8
  loss_weights:
    w_fourvec: 0.5634912488140177  # untuned: 0.3
    higgs_fourvec: 0.0
    higgs_mass: 0.0
    alpha_mmd: 0.0
    w_mass_mmd: 14.285222964496265  # untuned: 5.0
    w_mass: 0.0
    angular_mmd: 106.52384380300852  # untuned: 20.0
    dmet: 0.10533777560404485  # untuned: 0.05
    fourvec_bias: 8.92758029893128  # untuned: off
  batch_size: 8192
  epochs: 200
  learning_rate: 0.0003935722298809014  # untuned: 0.0004
  weight_decay: 2.2593535313788177e-06  # untuned: 0.03
  lr_plateau_factor: 0.5
  lr_plateau_patience: 8
  early_stopping_patience: 24
  early_stopping_min_delta: 0.0
  gradient_clip_val: 30.0
  adaptive_loss_weights: false
  log_loss_gradient_cosines: false
  d_model: 216
  n_heads: 8
  attention_blocks: 3
  attention_dropout: 0.1
  decoder_dropout: 0.2
  num_workers: 4
  persistent_workers: true
  pin_memory: true
  prefetch_factor: 2
  checkpoint_save_top_k: 1
  fourvec_loss: l1
mmd:
  alpha:
    kernel: imq
    bandwidths:
    - 2.0
    - 4.0
    - 8.0
  mass:
    kernel: imq
    bandwidths:
    - 0.02
    - 0.05
    - 0.1
  angular:
    kernel: imq
    bandwidths:
    - 0.6
    - 1.2
    - 2.4
paths:
  saved_path: /root/work/ww-pcres/fold_meta_ggF_tuned_v2
  data_path: /root/data/danning_h5/ypeng/mc20_qe_v61_recotruth_ggF_merged.h5
